# Normalize line endings. Shell scripts and the Dockerfile MUST stay LF —
# CRLF breaks the `#!/bin/sh` shebang inside the Linux container.
* text=auto
*.sh text eol=lf
Dockerfile text eol=lf
entrypoint.sh text eol=lf
